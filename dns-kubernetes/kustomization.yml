apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rhems-getssl

resources:
  - cronjob.yml
  - env.yml
  - rbac.yml

images:
  - name: rhems-getssl
    newName: rhemsjapan/rhems-getssl
    newTag: latest

configMapGenerator:
  - name: config
    files:
      - config.yml
